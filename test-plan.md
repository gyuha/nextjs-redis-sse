# Next.js Redis SSE 채팅 서비스 테스트 계획

## 테스트 목표
이 테스트 계획은 Next.js와 Redis, SSE를 활용한 실시간 채팅 서비스의 기능을 검증하기 위해 작성되었습니다. 주요 기능과 사용자 흐름을 테스트하여 애플리케이션이 요구사항을 충족하는지 확인합니다.

## 테스트 항목

### 1. 홈페이지 및 로그인 기능
- [x] 메인 페이지 접속 및 UI 요소 확인
- [x] 사용자 이름 또는 채널 선택 없이 제출 시 오류 처리
- [x] 유효한 사용자 이름과 채널 선택 시 채팅방으로 이동

### 2. 채팅 기능
- [x] 메시지 전송 및 표시
- [x] 다른 사용자에게 실시간으로 메시지 전달
- [x] 시스템 메시지(입장, 퇴장) 표시
- [x] 메시지 자동 스크롤

### 3. 채널 관리
- [x] 사이드바를 통한 채널 이동
- [x] 채널 변경 시 사용자 이름 유지
- [x] 채널별 메시지 분리 저장

### 4. 오류 처리 및 복구
- [x] 연결 끊김 시 자동 재연결
- [x] 서버 오류 발생 시 폴백 UI 표시
- [x] 메시지 전송 실패 시 오류 처리

## 테스트 환경
- **테스트 도구**: Playwright
- **브라우저**: Chrome, Firefox
- **Redis 서버**: 192.168.0.11:6379
- **로컬 개발 서버**: http://localhost:3000

## 테스트 케이스 상세 내용

### 테스트 케이스 1: 메인 페이지 접속 및 폼 확인
- 목적: 메인 페이지 접속 시 채팅 입장 폼이 정상적으로 표시되는지 확인
- 단계:
  1. 메인 페이지(/) 접속
  2. 제목 및 안내 메시지 확인
  3. 사용자 이름 입력 필드 확인
  4. 채널 선택 드롭다운 확인
  5. 채팅 입장 버튼 확인

### 테스트 케이스 2: 사용자 입력 오류 검증
- 목적: 사용자 입력 오류 시 적절한 오류 메시지가 표시되는지 확인
- 단계:
  1. 메인 페이지(/) 접속
  2. 입력 없이 채팅 입장 버튼 클릭
  3. 사용자 이름 오류 메시지 표시 확인
  4. 채널 선택 오류 메시지 표시 확인

### 테스트 케이스 3: 채팅방 입장 및 메시지 전송
- 목적: 채팅방에 입장하여 메시지를 주고받을 수 있는지 확인
- 단계:
  1. 첫 번째 사용자로 채팅방 입장
  2. 채팅방 UI 요소 확인
  3. 두 번째 사용자로 동일 채널 입장
  4. 첫 번째 사용자가 메시지 전송
  5. 두 번째 사용자 화면에 메시지 표시 확인
  6. 두 번째 사용자가 응답 메시지 전송
  7. 첫 번째 사용자 화면에 응답 메시지 표시 확인

### 테스트 케이스 4: 채널 이동
- 목적: 사이드바를 통해 채널 이동이 정상적으로 작동하는지 확인
- 단계:
  1. 채팅방 입장
  2. 현재 채널 확인
  3. 사이드바에서 다른 채널 클릭
  4. URL 변경 확인
  5. 새 채널 헤더 표시 확인

## 테스트 실행 방법
```bash
# 테스트 실행
pnpm dlx playwright test

# UI 모드로 테스트 실행
pnpm dlx playwright test --ui

# 특정 테스트 파일만 실행
pnpm dlx playwright test tests/chat.spec.ts

# 특정 브라우저에서만 실행
pnpm dlx playwright test --project=chromium
```

## 테스트 결과 및 이슈 추적
테스트 실행 후 발견된 이슈는 아래에 기록합니다:

| 이슈 ID | 설명 | 심각도 | 상태 |
|---------|------|--------|------|
| #1 | TBD | - | - |
| #2 | TBD | - | - |